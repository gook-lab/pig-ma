# 제안 (미채택)

**검토했지만 아직 채택하지 않은 설계.** 아카이브(=구현 완료)와 구분해서 둔다.
"언젠가 하려던 것"과 "이미 한 것"이 한 폴더에 섞이면 둘 다 못 믿게 된다.

| 제안 | 상태 (2026-08-22 확인) |
|---|---|
| `canvas-object-props-separation` | **미채택.** `CanvasObject` 는 여전히 flat 81필드 (`src/types.ts:348`) |
